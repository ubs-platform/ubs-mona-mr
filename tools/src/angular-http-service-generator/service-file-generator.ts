export class ServiceFileGenerator {
    static generateServiceFile(serviceName: string, methods: string[]): string {
        const className = `${serviceName}Service`;
        const head = "";
        return "";
    }
}
